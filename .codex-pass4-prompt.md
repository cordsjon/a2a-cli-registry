# tenet-bypass: repurpose — checked `ls .codex*prompt*` + find for *codex*pass*/*pass*prompt*; no prior scaffold persisted on disk (handover says reuse the pattern, not a file)

You are performing a FOURTH and final grounding pass on a design spec before it goes to
implementation planning. Your job is NOT to re-litigate settled design decisions — three
prior Codex passes already shaped this spec. Your job is to confirm the THREE 3rd-pass
fixes were correctly applied and sound, then hunt for any NEW hole that would break
implementation. End with a single verdict line.

## The spec to review (read it fully first)

`docs/superpowers/specs/2026-07-12-vocab01-goal-actions-redesign.md`

## Codebase to ground against (this repo: a2a-cli-registry)

Key files the spec references — verify the spec's claims against the ACTUAL code:
- `core/planner/search.py` (plan_chain, _hop_excluded, terminal predicate ~110-116,
  start-selection ~97-100, side-effect term gate ~67-69)
- `core/graph/edges.py` (edge formation, hub down-weight ~line 30)
- `core/ops_registry.py` (op input schema ~37, unknown-key rejection ~101)
- `core/catalog/queries.py::plan_cli_chain` (public wrapper)
- In the sibling repo `~/.hermes/hermes-adapter`: `hermes_adapter/tools/cli_registry.py`
  (_TAG_INFER_SYSTEM ~490, _infer_capability_tags ~545, discovery ~843, planner call ~899,
  tool schema ~387, bypass ~893, per-hop goal injection ~944)

## What the 3rd pass fixed — CONFIRM each is correctly applied AND sound

For each, answer: (a) is the fix actually present in the spec text? (b) does it hold up
against the real code? (c) does it fully close the hole the 3rd pass identified?

1. **§2.2 — disjoint tag sets for `_ACTION_REQUIRES_TAG`.** The 3rd pass found overlapping
   tag sets re-introduce ambiguity (webhook including `notify` → send_mail falsely matches
   webhook). Confirm the map is pairwise disjoint, that `email→{send}` uniquely matches the
   live `send_mail` row, and that the disjointness invariant is enforceable by a unit test
   against live tags. Check the live `send_mail` intent_tags actually contain `send`.

2. **§2.6 — compound-goal start gate.** The 3rd pass found admitting action terminals as
   BFS start nodes for a compound goal (non-empty goal_outputs) is unsafe (terminal A's
   confirmation text feeds terminal B, falsely satisfying the predicate). Confirm the gate
   is `not goal_out` (empty goal_outputs only), NOT merely `goal_actions` non-empty.

3. **§2.7 — slug-scoped, final-position self-authorization.** The 3rd pass found unioning a
   terminal's enum into a class-wide `allow_side_effects` would admit all 32 live writes-fs
   CLIs as mid-chain hops (security regression). Confirm self-authorization is scoped to the
   exact matched slugs AND only at final position, that `_hop_excluded`'s signature is
   unchanged, and that the wrapping (`excluded = _hop_excluded(...) and not (is_final and
   slug in action_terminals)`) is correct — no mid-chain leak.

4. **§2.8 — shared-vocab contract, no cross-repo duplication.** The 3rd pass found the spec
   implied the adapter must duplicate `_ACTION_REQUIRES_TAG`. Confirm validation lives ONLY
   in the registry, the adapter forwards verbatim + learns known verbs from the structured
   error payload, and this reuses the existing vocab-reject retry loop (not a new mechanism).
   Check ops_registry.py:101 actually returns string errors on the unknown-key path as claimed.

## New-hole hunt (be adversarial, but only on things that break IMPLEMENTATION)

- §3's plumbing table claims 5 omissions are now enumerated (ops schema @101, tool schema
  @387, empty-goal_outputs discovery crash @843, planner call, tag inference). Is any
  plumbing site STILL missing? Grep for other consumers of `goal_outputs[0]` or the
  plan_cli_chain input schema that the table omits.
- §2.5 terminal edge synthesis: does synthesizing `(P, T, H)` in-memory actually get
  traversed by the BFS given the §2.4 short-circuit edit? Trace one path.
- §8 ordering: is "registry first, adapter atomically" actually safe against a partial
  deploy, or is there a hidden ordering dependency?
- Any AC (AC-02..AC-05) that is not actually testable as written?

## Output format (REQUIRED)

Write plain prose findings, then end with EXACTLY ONE of these two lines as the final line:

`PLAN-READY`

or

`NOT-PLAN-READY`

If NOT-PLAN-READY, precede it with a numbered list of the specific remaining holes, each
with the spec section and the concrete code fact that contradicts it.
